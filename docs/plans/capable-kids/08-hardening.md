# Day 8 — Production hardening & verification

- **8.1** Risk/blocker register sweep — resolve everything in 00-roadmap's register.
- **8.2** Robustness — empty/loading/error states on every new page; auth gating audited; input
  validation on every new endpoint; mobile pass.
- **8.3** Tests — fill coverage gaps across server registries/routes and key client logic.
- **8.4** Full verification — run the app, walk the entire flow (baseline → radar → library → track →
  dashboard) for both a parent and a kid login; run the full test suite.

## Commit
`chore(capabilities): production hardening + full-flow verification`
